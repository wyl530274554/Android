<resources xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.NoActionBar" tools:ignore="NewApi">
        <item name="colorPrimary">@color/color_app_primary</item>
        <item name="colorPrimaryDark">@color/color_app_primary_dark</item>
        <item name="colorAccent">@color/color_app_primary</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- 整个应用的基本主题 -->
    <style name="MyTheme.Application" parent="@style/AppBaseTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/SlideRightAnimation</item>
    </style>

</resources>
